# Renuka
# This script is used to convert the CSV files provided into a SQLite database for the AI Agent.

import pandas as pd
import sqlite3

# Load CSVs (manually load your own CSVs here)
# df = pd.read_csv("path_to_csv.csv")

# Clean and insert into database
# conn = sqlite3.connect("data/ecommerce.db")
# df.to_sql("table_name", conn, if_exists="replace", index=False)

# conn.close()
